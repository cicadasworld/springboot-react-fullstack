pluginManagement {
    repositories {
        maven(url = uri("../mvn-repo"))
    }
}

dependencyResolutionManagement {
    repositories {
        maven(url = uri("../mvn-repo"))
    }
}

rootProject.name = "todo-management"
